---
import { getCollection } from 'astro:content';
import Header from '../components/Header.astro';
import PostCard from '../components/PostCard.astro';
import SubscribeForm from '../components/SubscribeForm.astro';
import CoffeeCup from '../components/CoffeeCup.astro';
import '../styles/global.css';

const all   = await getCollection('posts');
const posts = all.sort((a,b) => b.data.date.getTime() - a.data.date.getTime());
const [featured, ...rest] = posts;
const total = posts.length;
const allTags = [...new Set(posts.flatMap(p => p.data.tags))].sort();

const tagCounts: Record<string, number> = {};
posts.forEach(p => p.data.tags.forEach(t => { tagCounts[t] = (tagCounts[t] ?? 0) + 1; }));
const topTags = Object.entries(tagCounts).sort((a, b) => b[1] - a[1]).slice(0, 7).map(([t]) => t);

const T = 'First Cup — IA, Dev & Tech';
const D = 'Tu café tech de cada mañana. IA, desarrollo y tecnología en tu inbox.';
const siteUrl = Astro.site?.toString().replace(/\/$/, '') ?? 'https://firstcup.dev';
const ogImg = `${siteUrl}/og-default.svg`;
---

<!doctype html>
<html lang="es">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <meta name="description" content={D}/>
  <meta property="og:type" content="website"/>
  <meta property="og:url" content={`${siteUrl}/`}/>
  <meta property="og:title" content={T}/>
  <meta property="og:description" content={D}/>
  <meta property="og:image" content={ogImg}/>
  <meta property="og:site_name" content="First Cup"/>
  <meta name="twitter:card" content="summary_large_image"/>
  <meta name="twitter:title" content={T}/>
  <meta name="twitter:description" content={D}/>
  <meta name="twitter:image" content={ogImg}/>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg"/>
  <link rel="canonical" href={`${siteUrl}/`}/>
  <link rel="sitemap" href="/sitemap.xml"/>
  <link rel="alternate" type="application/rss+xml" title="First Cup RSS" href="/rss.xml"/>
  <title>{T}</title>
</head>

<body>
<Header/>

<main>

  <!-- ════════════ HERO ════════════ -->
  <section class="hero">

    <div class="hero-bg-amber" aria-hidden="true"></div>
    <div class="hero-bg-grid"  aria-hidden="true"></div>

    <div class="container hero-container">
      <div class="hero-body">

        <!-- LEFT: text content -->
        <div class="hero-text">

          <!-- pill eyebrow -->
          <div class="eyebrow-pill anim-up" style="animation-delay:0ms">
            <span class="pulse"></span>
            Newsletter · IA &amp; Desarrollo
          </div>

          <!-- headline -->
          <h1 class="headline">
            <span class="hl-line anim-up" style="animation-delay:60ms">
              Todo lo que importa
            </span>
            <span class="hl-line hl-accent anim-up" style="animation-delay:130ms">
              en tech,
            </span>
            <span class="hl-line hl-small anim-up" style="animation-delay:200ms">
              sin el ruido.
            </span>
          </h1>

          <!-- sub -->
          <p class="hero-sub anim-up" style="animation-delay:280ms">
            Un agente de IA analiza cada mañana GitHub Trending, Hacker News, ArXiv,
            Reddit y los mejores blogs del sector — y te entrega solo lo que merece
            tu atención.
          </p>

          <!-- feature pills -->
          <div class="hero-features anim-up" style="animation-delay:340ms">
            <span class="hf-item">
              <svg width="13" height="13" viewBox="0 0 13 13" fill="none">
                <circle cx="6.5" cy="6.5" r="5.5" stroke="currentColor" stroke-width="1.2"/>
                <path d="M4 6.5l1.8 1.8L9 4.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              15+ fuentes curadas
            </span>
            <span class="hf-sep"></span>
            <span class="hf-item">
              <svg width="13" height="13" viewBox="0 0 13 13" fill="none">
                <circle cx="6.5" cy="6.5" r="5.5" stroke="currentColor" stroke-width="1.2"/>
                <path d="M4 6.5l1.8 1.8L9 4.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              Publicado cada mañana
            </span>
            <span class="hf-sep"></span>
            <span class="hf-item">
              <svg width="13" height="13" viewBox="0 0 13 13" fill="none">
                <circle cx="6.5" cy="6.5" r="5.5" stroke="currentColor" stroke-width="1.2"/>
                <path d="M4 6.5l1.8 1.8L9 4.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              100% automatizado
            </span>
          </div>

          <!-- stats row -->
          <div class="hero-stats anim-up" style="animation-delay:400ms">
            <div class="hstat">
              <span class="hstat-n">
                <span class="count" data-target="15">0</span><span class="hstat-s">+</span>
              </span>
              <span class="hstat-l">fuentes diarias</span>
            </div>
            <div class="hstat-sep"></div>
            <div class="hstat">
              <span class="hstat-n">
                <span class="count" data-target={total}>0</span>
              </span>
              <span class="hstat-l">posts publicados</span>
            </div>
            <div class="hstat-sep"></div>
            <div class="hstat">
              <span class="hstat-n">
                <span class="count" data-target="100">0</span><span class="hstat-s">%</span>
              </span>
              <span class="hstat-l">IA generado</span>
            </div>
          </div>

          <!-- CTA row -->
          <div class="hero-actions anim-up" style="animation-delay:470ms">
            <a href="/subscribe" class="hero-cta">
              Suscribirme gratis
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                <path d="M3 7h8M8 4l3 3-3 3" stroke="currentColor" stroke-width="1.5"
                  stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <a href="#posts" class="hero-ghost">Ver los posts →</a>
          </div>

          <!-- Topic tags -->
          {topTags.length > 0 && (
            <div class="hero-topics anim-up" style="animation-delay:530ms">
              <span class="hero-topics-label">Temas:</span>
              {topTags.map(t => (
                <button class="hero-topic" data-topic={t}>{t}</button>
              ))}
            </div>
          )}

        </div>

        <!-- RIGHT: digest preview card -->
        <div class="hero-visual anim-in" style="animation-delay:260ms">
          <div class="hv-card">
            <div class="hv-ring hv-ring-1" aria-hidden="true"></div>
            <div class="hv-ring hv-ring-2" aria-hidden="true"></div>

            <!-- card header -->
            <div class="hv-head">
              <div class="hv-brand-block">
                <span class="hv-icon">☕</span>
                <span class="hv-brand-name">First Cup</span>
              </div>
              <span class="hv-live">
                <span class="hv-dot" aria-hidden="true"></span>
                En vivo
              </span>
            </div>

            <!-- coffee cup -->
            <div class="hv-cup">
              <CoffeeCup />
            </div>

            <!-- source pills -->
            <div class="hv-sources-row">
              <span class="hvs-label">Fuentes analizadas</span>
              <div class="hv-sources">
                <span class="hvs-chip hvs-active">HN</span>
                <span class="hvs-chip hvs-active">GitHub</span>
                <span class="hvs-chip hvs-active">arXiv</span>
                <span class="hvs-chip">Dev.to</span>
                <span class="hvs-chip">RSS</span>
                <span class="hvs-chip">+10</span>
              </div>
            </div>

            <!-- divider -->
            <div class="hv-divider"></div>

            <!-- next run -->
            <div class="hv-schedule">
              <div class="hv-sched-row">
                <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
                  <circle cx="6" cy="6" r="5" stroke="currentColor" stroke-width="1.2" opacity=".5"/>
                  <path d="M6 3v3l2 1.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" opacity=".7"/>
                </svg>
                <span class="hv-sched-text">Próxima edición · <strong>07:00</strong></span>
              </div>
              <div class="hv-sched-row">
                <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
                  <path d="M2 6h8M6 2l4 4-4 4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" opacity=".5"/>
                </svg>
                <span class="hv-sched-text">Publicado automáticamente</span>
              </div>
            </div>

          </div>
        </div>

      </div>
    </div>

    <div class="hero-rule"></div>
  </section>

  <!-- ════════════ SOURCES TICKER ════════════ -->
  <div class="ticker" aria-hidden="true">
    <div class="ticker-fade ticker-fade-l"></div>
    <div class="ticker-inner">
      <div class="ticker-track">
        <span class="t-item">GitHub Trending</span><span class="t-sep">·</span>
        <span class="t-item">Hacker News</span><span class="t-sep">·</span>
        <span class="t-item">ArXiv Papers</span><span class="t-sep">·</span>
        <span class="t-item">Reddit r/ML</span><span class="t-sep">·</span>
        <span class="t-item">Dev.to</span><span class="t-sep">·</span>
        <span class="t-item">Product Hunt</span><span class="t-sep">·</span>
        <span class="t-item">GitHub API</span><span class="t-sep">·</span>
        <span class="t-item">RSS Feeds</span><span class="t-sep">·</span>
        <!-- duplicate for seamless loop -->
        <span class="t-item">GitHub Trending</span><span class="t-sep">·</span>
        <span class="t-item">Hacker News</span><span class="t-sep">·</span>
        <span class="t-item">ArXiv Papers</span><span class="t-sep">·</span>
        <span class="t-item">Reddit r/ML</span><span class="t-sep">·</span>
        <span class="t-item">Dev.to</span><span class="t-sep">·</span>
        <span class="t-item">Product Hunt</span><span class="t-sep">·</span>
        <span class="t-item">GitHub API</span><span class="t-sep">·</span>
        <span class="t-item">RSS Feeds</span><span class="t-sep">·</span>
      </div>
    </div>
    <div class="ticker-fade ticker-fade-r"></div>
  </div>


  <!-- ════════════ POSTS ════════════ -->
  <section class="posts-section" id="posts">
    <div class="container">

      {posts.length === 0 ? (
        <div class="empty anim-up">
          <p class="empty-label">Próximamente</p>
          <h2 class="empty-h">El primer digest llega pronto</h2>
          <p class="empty-p">Suscríbete para recibirlo cuando salga.</p>
          <a href="/subscribe" class="pill-btn">Suscribirme gratis</a>
        </div>
      ) : (
        <>
          {/* Featured post */}
          {featured && (
            <div class="featured-wrap anim-up" style="animation-delay:50ms">
              <p class="section-label">Destacado</p>
              <PostCard
                title={featured.data.title}
                slug={featured.slug}
                date={featured.data.date}
                description={featured.data.description}
                tags={featured.data.tags}
                featured={true}
                editionNum={total}
                cover={featured.data.cover}
              />
            </div>
          )}

          {/* Grid of the rest */}
          {rest.length > 0 && (
            <div class="grid-wrap">
              <p class="section-label anim-up" style="animation-delay:80ms">
                Publicaciones
                <span class="section-count">{rest.length}</span>
              </p>
              {allTags.length > 0 && (
                <div class="tag-filter" id="tag-filter">
                  <button class="tf-btn active" data-tag="all">Todos</button>
                  {allTags.map(t => (
                    <button class="tf-btn" data-tag={t}>{t}</button>
                  ))}
                </div>
              )}
              <div class="posts-grid" id="posts-grid">
                {rest.map((p, i) => (
                  <PostCard
                    title={p.data.title}
                    slug={p.slug}
                    date={p.data.date}
                    description={p.data.description}
                    tags={p.data.tags}
                    index={i}
                    editionNum={total - 1 - i}
                    cover={p.data.cover}
                  />
                ))}
              </div>
            </div>
          )}
        </>
      )}

    </div>
  </section>


  <!-- ════════════ NEWSLETTER ════════════ -->
  <section class="nl">
    <div class="container">
      <div class="nl-in">

        <div class="nl-left">
          <span class="nl-badge">Newsletter · Free</span>
          <h2 class="nl-h">
            Todo lo importante,<br/>
            <em>en tu inbox.</em>
          </h2>
          <p class="nl-p">
            Sin spam. Sin relleno. Cancela cuando quieras.
          </p>
          <ul class="nl-list">
            <li>GitHub Trending diario</li>
            <li>Papers de ArXiv en lenguaje humano</li>
            <li>Hacker News + Reddit curado</li>
            <li>Los mejores blogs del sector</li>
          </ul>
        </div>

        <div class="nl-right">
          <SubscribeForm compact={false}/>
        </div>

      </div>
    </div>
  </section>

</main>

<!-- ════════════ FOOTER ════════════ -->
<footer class="foot">
  <div class="container foot-in">
    <span class="foot-brand">
      <span class="foot-n">FC</span>
      First Cup
    </span>
    <nav class="foot-nav">
      <a href="/">Posts</a>
      <a href="/subscribe">Suscribirse</a>
    </nav>
    <span class="foot-c">© {new Date().getFullYear()} · Hecho con Astro</span>
  </div>
</footer>

<script>
  // ── Scroll progress bar ──
  const bar = document.getElementById('scroll-progress');
  const onScroll = () => {
    if (!bar) return;
    const d = document.documentElement;
    bar.style.width = (d.scrollTop / (d.scrollHeight - d.clientHeight) * 100) + '%';
  };
  window.addEventListener('scroll', onScroll, { passive: true });

  // ── Stats count-up on scroll ──
  const countEls = document.querySelectorAll<HTMLElement>('.count[data-target]');
  const countObs = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      const el = entry.target as HTMLElement;
      const target = parseInt(el.dataset.target ?? '0');
      const duration = 1400;
      const start = performance.now();
      const tick = (now: number) => {
        const p = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - p, 3); // ease-out cubic
        el.textContent = Math.round(eased * target).toString();
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      countObs.unobserve(el);
    });
  }, { threshold: 0.6 });
  countEls.forEach(el => countObs.observe(el));

  // ── Card scroll reveal (staggered) ──
  const cards = document.querySelectorAll<HTMLElement>('.card');
  const cardObs = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      (entry.target as HTMLElement).classList.add('in-view');
      cardObs.unobserve(entry.target);
    });
  }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
  cards.forEach(c => cardObs.observe(c));

  // ── Magnetic buttons ──
  document.querySelectorAll<HTMLElement>('.hero-cta, .ncta').forEach(btn => {
    btn.addEventListener('mousemove', (e: MouseEvent) => {
      const r = btn.getBoundingClientRect();
      const dx = (e.clientX - (r.left + r.width / 2)) * 0.28;
      const dy = (e.clientY - (r.top + r.height / 2)) * 0.28;
      btn.style.transition = 'transform 0.1s ease';
      btn.style.transform = `translate(${dx}px, ${dy}px)`;
    });
    btn.addEventListener('mouseleave', () => {
      btn.style.transition = 'transform 0.5s var(--spring, cubic-bezier(.16,1,.3,1))';
      btn.style.transform = '';
    });
  });

  // ── Hero topic tags → scroll + filter ──
  document.querySelectorAll<HTMLButtonElement>('.hero-topic').forEach(btn => {
    btn.addEventListener('click', () => {
      const tag = btn.dataset.topic ?? '';
      const filterBtn = document.querySelector<HTMLButtonElement>(`.tf-btn[data-tag="${tag}"]`);
      if (filterBtn) {
        filterBtn.click();
        document.getElementById('posts')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });

  // ── Tag filter ──
  const filterBtns = document.querySelectorAll<HTMLButtonElement>('.tf-btn');
  filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      filterBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      const tag = btn.dataset.tag ?? 'all';
      document.querySelectorAll<HTMLElement>('.card:not(.card--featured)').forEach(card => {
        const tags = card.getAttribute('data-tags')?.split(',') ?? [];
        card.style.display = tag === 'all' || tags.includes(tag) ? '' : 'none';
      });
      // Featured card
      const featuredCard = document.querySelector<HTMLElement>('.card--featured');
      if (featuredCard) {
        const tags = featuredCard.getAttribute('data-tags')?.split(',') ?? [];
        const wrap = featuredCard.closest<HTMLElement>('.featured-wrap');
        if (wrap) wrap.style.display = tag === 'all' || tags.includes(tag) ? '' : 'none';
      }
    });
  });

  // ── Steam cursor particles ──
  let lastParticle = 0;
  document.addEventListener('mousemove', (e: MouseEvent) => {
    const now = Date.now();
    if (now - lastParticle < 55) return;
    lastParticle = now;
    const p = document.createElement('div');
    p.className = 'steam-particle';
    const ox = (Math.random() - 0.5) * 12;
    const oy = (Math.random() - 0.5) * 8;
    p.style.left = (e.clientX + ox) + 'px';
    p.style.top  = (e.clientY + oy) + 'px';
    const dx = (Math.random() - 0.5) * 18;
    document.body.appendChild(p);
    p.animate([
      { opacity: 0.5, transform: 'translate(-50%,-50%) scale(1)' },
      { opacity: 0,   transform: `translate(calc(-50% + ${dx}px), calc(-50% - 28px)) scale(0.2)` }
    ], { duration: 950, easing: 'ease-out', fill: 'forwards' })
      .addEventListener('finish', () => p.remove());
  });
</script>

</body>
</html>

<style>

  /* ── HERO ── */
  .hero {
    padding: 5.5rem 0 0;
    position: relative;
    overflow: hidden;
  }

  .hero-container { position: relative; z-index: 1; }

  .hero-bg-amber {
    position: absolute;
    top: -80px; left: -80px;
    width: 640px; height: 520px;
    background: radial-gradient(ellipse, rgba(92,47,14,0.06) 0%, transparent 65%);
    pointer-events: none;
  }

  .hero-bg-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(92,47,14,.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(92,47,14,.04) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 0%, black 0%, transparent 100%);
    -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 0%, black 0%, transparent 100%);
  }

  .hero-body {
    display: grid;
    grid-template-columns: 56fr 44fr;
    gap: 4rem;
    align-items: center;
    padding-bottom: 4.5rem;
  }

  .hero-text { display: flex; flex-direction: column; }

  /* Pill eyebrow */
  .eyebrow-pill {
    display: inline-flex;
    align-items: center;
    gap: .55rem;
    background: rgba(201,165,116,0.08);
    border: 1px solid rgba(201,165,116,0.28);
    border-radius: 100px;
    padding: .38rem .9rem;
    font-family: var(--font-mono);
    font-size: .65rem;
    color: var(--accent);
    letter-spacing: .06em;
    text-transform: uppercase;
    width: fit-content;
    margin-bottom: 1.75rem;
  }

  .pulse {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--amber);
    box-shadow: 0 0 6px var(--amber-glow);
    animation: breathe 2.5s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes breathe {
    0%,100% { transform:scale(1); opacity:1; }
    50%      { transform:scale(.6); opacity:.4; }
  }

  .headline {
    display: flex;
    flex-direction: column;
    gap: .02em;
    margin-bottom: 1.1rem;
  }

  .hl-line {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(2.8rem, 6.2vw, 4.8rem);
    font-weight: 400;
    line-height: 1.06;
    letter-spacing: -.025em;
    color: var(--text);
  }

  .hl-accent {
    font-style: italic;
    color: var(--amber);
    font-size: clamp(3.2rem, 7.5vw, 6rem);
    line-height: 1.0;
  }

  .hl-small {
    font-size: clamp(2rem, 4.2vw, 3.4rem);
    color: var(--text-2);
    font-weight: 400;
  }

  /* Steam cursor particles */
  :global(.steam-particle) {
    position: fixed;
    width: 5px; height: 5px;
    border-radius: 50%;
    background: rgba(92,47,14,0.22);
    pointer-events: none;
    z-index: 9995;
    filter: blur(1.5px);
  }

  .hero-sub {
    font-size: .96rem;
    color: var(--text-2);
    line-height: 1.85;
    max-width: 480px;
    margin-bottom: 1.5rem;
  }

  /* Feature pills row */
  .hero-features {
    display: flex;
    align-items: center;
    gap: .5rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .hf-item {
    display: inline-flex;
    align-items: center;
    gap: .35rem;
    font-family: var(--font-mono);
    font-size: .64rem;
    color: var(--text-2);
    letter-spacing: .02em;
  }
  .hf-item svg { color: var(--amber); flex-shrink: 0; }

  .hf-sep {
    width: 1px; height: 12px;
    background: var(--border-mid);
    flex-shrink: 0;
  }

  /* Stats row */
  .hero-stats {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2.25rem;
    padding: 1rem 1.25rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    width: fit-content;
  }

  .hstat { display: flex; flex-direction: column; gap: .1rem; }

  .hstat-n {
    font-family: var(--font-display);
    font-size: 1.55rem;
    font-weight: 400;
    color: var(--text);
    line-height: 1;
    display: flex;
    align-items: baseline;
    gap: .05em;
  }

  .hstat-s {
    font-size: .8rem;
    color: var(--accent);
    font-family: var(--font-body);
    font-weight: 600;
  }

  .hstat-l {
    font-family: var(--font-mono);
    font-size: .58rem;
    color: var(--text-3);
    text-transform: uppercase;
    letter-spacing: .07em;
    margin-top: .15rem;
  }

  .hstat-sep {
    width: 1px;
    height: 32px;
    background: var(--border-mid);
    flex-shrink: 0;
  }

  /* CTA row */
  .hero-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.75rem;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    gap: .5rem;
    background: var(--accent);
    color: var(--bg);
    font-weight: 600;
    font-size: .9rem;
    padding: .8rem 1.6rem;
    border-radius: 8px;
    text-decoration: none;
    transition: opacity .15s, transform .25s var(--spring), box-shadow .25s;
  }
  .hero-cta:hover {
    opacity: .88;
    transform: translateY(-2px);
    box-shadow: 0 8px 28px rgba(92,47,14,0.28);
    text-decoration: none;
  }
  .hero-cta svg { transition: transform .25s var(--spring); }
  .hero-cta:hover svg { transform: translateX(3px); }

  .hero-ghost {
    font-size: .875rem;
    font-weight: 500;
    color: var(--text-2);
    text-decoration: none;
    transition: color .15s;
  }
  .hero-ghost:hover { color: var(--text); text-decoration: none; }

  /* ── COFFEE CARD WIDGET ── */
  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding-top: 1.5rem;
  }

  .hv-card {
    position: relative;
    background: #1C0E05;
    border-radius: 24px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0;
    width: 296px;
    overflow: hidden;
    border: 1px solid rgba(255,255,255,.08);
    box-shadow:
      0 0 0 1px rgba(92,47,14,.12),
      0 40px 80px rgba(18,10,3,.3),
      0 12px 28px rgba(18,10,3,.2),
      inset 0 1px 0 rgba(255,255,255,.06);
  }

  .hv-card::before {
    content: '';
    position: absolute;
    top: 45%; left: 50%;
    transform: translate(-50%, -50%);
    width: 260px; height: 260px;
    background: radial-gradient(circle, rgba(139,85,51,.18) 0%, transparent 65%);
    pointer-events: none;
    z-index: 0;
  }

  .hv-ring {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,.04);
    pointer-events: none;
    top: 44%; left: 50%;
  }
  .hv-ring-1 { width: 220px; height: 220px; transform: translate(-50%, -52%); }
  .hv-ring-2 { width: 360px; height: 360px; transform: translate(-50%, -52%); }

  .hv-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    z-index: 1;
    margin-bottom: 1rem;
  }

  .hv-brand-block {
    display: flex;
    align-items: center;
    gap: .4rem;
  }

  .hv-icon {
    font-size: .95rem;
    line-height: 1;
  }

  .hv-brand-name {
    font-family: var(--font-display);
    font-size: .82rem;
    font-weight: 600;
    color: rgba(245,238,216,.65);
    letter-spacing: -.01em;
  }

  .hv-live {
    display: inline-flex;
    align-items: center;
    gap: .3rem;
    font-family: var(--font-mono);
    font-size: .56rem;
    color: #4ade80;
    background: rgba(74,222,128,.1);
    border: 1px solid rgba(74,222,128,.22);
    border-radius: 4px;
    padding: .1rem .42rem;
    letter-spacing: .06em;
    text-transform: uppercase;
  }

  .hv-dot {
    width: 5px; height: 5px;
    border-radius: 50%;
    background: #4ade80;
    flex-shrink: 0;
    animation: breathe 2.5s ease-in-out infinite;
  }

  .hv-cup {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 0 1rem;
  }

  .hv-cup :global(.cup-wrap) {
    width: 150px;
    height: 150px;
  }

  /* Sources row */
  .hv-sources-row {
    position: relative;
    z-index: 1;
    margin-bottom: 1rem;
  }

  .hvs-label {
    display: block;
    font-family: var(--font-mono);
    font-size: .54rem;
    color: rgba(245,238,216,.3);
    text-transform: uppercase;
    letter-spacing: .08em;
    margin-bottom: .45rem;
  }

  .hv-sources {
    display: flex;
    gap: .3rem;
    flex-wrap: wrap;
  }

  .hvs-chip {
    font-family: var(--font-mono);
    font-size: .55rem;
    border-radius: 4px;
    padding: .1rem .35rem;
    letter-spacing: .04em;
    text-transform: uppercase;
    color: rgba(245,238,216,.35);
    background: rgba(255,255,255,.04);
    border: 1px solid rgba(255,255,255,.07);
    transition: all .2s;
  }

  .hvs-chip.hvs-active {
    color: rgba(245,238,216,.75);
    background: rgba(139,85,51,.2);
    border-color: rgba(139,85,51,.35);
  }

  .hv-divider {
    height: 1px;
    background: rgba(255,255,255,.06);
    margin: 0 0 1rem;
    position: relative;
    z-index: 1;
  }

  /* Schedule row */
  .hv-schedule {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: .45rem;
  }

  .hv-sched-row {
    display: flex;
    align-items: center;
    gap: .45rem;
  }

  .hv-sched-row svg { color: rgba(245,238,216,.4); flex-shrink: 0; }

  .hv-sched-text {
    font-family: var(--font-mono);
    font-size: .62rem;
    color: rgba(245,238,216,.45);
    letter-spacing: .02em;
  }

  .hv-sched-text strong {
    color: var(--amber);
    font-weight: 600;
  }

  /* Topic tags */
  .hero-topics {
    display: flex;
    align-items: center;
    gap: .4rem;
    flex-wrap: wrap;
  }

  .hero-topics-label {
    font-family: var(--font-mono);
    font-size: .63rem;
    color: var(--text-3);
    letter-spacing: .06em;
    text-transform: uppercase;
    flex-shrink: 0;
  }

  .hero-topic {
    font-family: var(--font-mono);
    font-size: .68rem;
    color: var(--text-2);
    background: none;
    border: 1px solid var(--border-mid);
    border-radius: 4px;
    padding: .2rem .55rem;
    cursor: pointer;
    transition: color .15s, border-color .15s, background .15s;
    letter-spacing: .02em;
  }
  .hero-topic:hover {
    color: var(--accent);
    border-color: rgba(92,47,14,.4);
    background: rgba(92,47,14,.06);
  }

  .hero-rule {
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, var(--border-mid) 30%, transparent 70%);
  }

  /* ── SOURCES TICKER ── */
  .ticker {
    position: relative;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: .55rem 0;
    overflow: hidden;
    background: var(--bg-card);
  }

  .ticker-inner { overflow: hidden; }

  .ticker-track {
    display: flex;
    align-items: center;
    gap: 1.8rem;
    width: max-content;
    animation: tickerScroll 28s linear infinite;
  }
  .ticker-track:hover { animation-play-state: paused; }

  @keyframes tickerScroll {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }

  .t-item {
    font-family: var(--font-mono);
    font-size: .62rem;
    color: var(--text-3);
    text-transform: uppercase;
    letter-spacing: .09em;
    white-space: nowrap;
    transition: color .15s;
  }
  .ticker-track:hover .t-item { color: var(--text-2); }

  .t-sep {
    color: var(--accent);
    font-size: .75rem;
    opacity: .5;
    flex-shrink: 0;
  }

  .ticker-fade {
    position: absolute;
    top: 0; bottom: 0;
    width: 80px;
    z-index: 1;
    pointer-events: none;
  }
  .ticker-fade-l {
    left: 0;
    background: linear-gradient(90deg, var(--bg-card) 0%, transparent 100%);
  }
  .ticker-fade-r {
    right: 0;
    background: linear-gradient(270deg, var(--bg-card) 0%, transparent 100%);
  }


  /* ── TAG FILTER ── */
  .tag-filter {
    display: flex;
    align-items: center;
    gap: .4rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }

  .tf-btn {
    font-family: var(--font-mono);
    font-size: .68rem;
    font-weight: 500;
    color: var(--text-2);
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 5px;
    padding: .28rem .65rem;
    cursor: pointer;
    transition: color .15s, border-color .15s, background .15s;
    letter-spacing: .03em;
    text-transform: lowercase;
  }
  .tf-btn:hover {
    color: var(--amber);
    border-color: rgba(201,165,116,.35);
    background: var(--amber-dim);
  }
  .tf-btn.active {
    color: #1A120A;
    background: var(--accent);
    border-color: var(--accent);
  }

  /* ── POSTS ── */
  .posts-section { padding: 3.5rem 0 5rem; }

  .section-label {
    display: flex;
    align-items: center;
    gap: .6rem;
    font-family: var(--font-mono);
    font-size: .67rem;
    font-weight: 500;
    color: var(--text-2);
    text-transform: uppercase;
    letter-spacing: .1em;
    margin-bottom: 1.25rem;
  }

  .section-count {
    font-size: .65rem;
    color: var(--accent);
    background: var(--amber-dim);
    border: 1px solid rgba(201,165,116,.22);
    border-radius: 3px;
    padding: .08rem .4rem;
  }

  .featured-wrap { margin-bottom: 3.5rem; }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.1rem;
  }

  /* ── Empty ── */
  .empty {
    padding: 5rem 0;
    border-top: 1px solid var(--border);
    max-width: 480px;
  }
  .empty-label {
    font-family: var(--font-mono);
    font-size: .68rem;
    color: var(--amber);
    text-transform: uppercase;
    letter-spacing: .09em;
    margin-bottom: .75rem;
  }
  .empty-h {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -.02em;
    margin-bottom: .5rem;
  }
  .empty-p { color: var(--text-2); font-size: .9rem; line-height: 1.65; margin-bottom: 1.5rem; }
  .pill-btn {
    display: inline-block;
    background: var(--accent);
    color: var(--bg);
    font-weight: 700;
    font-size: .875rem;
    padding: .6rem 1.35rem;
    border-radius: 7px;
    text-decoration: none;
    transition: opacity .15s;
  }
  .pill-btn:hover { opacity: .85; text-decoration: none; }


  /* ── NEWSLETTER ── */
  .nl { padding-bottom: 6rem; }

  .nl-in {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: start;
    padding: 3.5rem;
    border: 1px solid var(--border-mid);
    border-radius: 14px;
    background: var(--surface);
    position: relative;
    overflow: hidden;
  }

  /* amber top accent line */
  .nl-in::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, var(--amber) 0%, transparent 55%);
    opacity: .6;
  }

  .nl-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: .66rem;
    color: var(--accent);
    border: 1px solid rgba(201,165,116,.28);
    border-radius: 3px;
    padding: .12rem .5rem;
    letter-spacing: .06em;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .nl-h {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 900;
    color: var(--text);
    letter-spacing: -.03em;
    line-height: 1.15;
    margin-bottom: .75rem;
  }
  .nl-h em { font-style: normal; color: var(--amber); font-weight: 500; }

  .nl-p { color: var(--text-2); font-size: .875rem; line-height: 1.7; margin-bottom: 1.5rem; }

  .nl-list { list-style: none; display: flex; flex-direction: column; gap: .4rem; }
  .nl-list li {
    font-size: .85rem; color: var(--text-2);
    padding-left: 1rem; position: relative;
  }
  .nl-list li::before {
    content: '·';
    position: absolute; left: 0;
    color: var(--amber);
    font-size: 1rem; line-height: 1.3;
  }

  .nl-right { padding-top: 4rem; }


  /* ── FOOTER ── */
  .foot { border-top: 1px solid var(--border); padding: 1.5rem 0; }

  .foot-in {
    display: flex; align-items: center;
    justify-content: space-between;
    flex-wrap: wrap; gap: 1rem;
  }

  .foot-brand {
    display: flex; align-items: center; gap: .45rem;
    font-family: var(--font-display);
    font-size: .85rem; font-weight: 700;
    color: var(--text-2);
  }

  .foot-n {
    width: 28px; height: 22px;
    background: var(--accent); color: var(--bg);
    font-size: .62rem; font-weight: 600;
    border-radius: 4px;
    display: inline-flex; align-items: center; justify-content: center;
  }

  .foot-nav { display: flex; gap: 1.5rem; }
  .foot-nav a { font-size: .82rem; color: var(--text-2); transition: color .15s; }
  .foot-nav a:hover { color: var(--text); }

  .foot-c { font-size: .74rem; color: var(--text-3); }


  /* ── RESPONSIVE ── */
  @media (max-width: 860px) {
    .hero-body {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .hero-visual { order: -1; padding-top: 0; }
    .hv-card { width: 100%; max-width: 320px; }
    .hv-cup :global(.cup-wrap) { width: 120px; height: 120px; }
    .hero-stats { gap: 1rem; }
    .posts-grid { grid-template-columns: repeat(2,1fr); }
    .nl-in { grid-template-columns: 1fr; gap: 2rem; padding: 2rem; }
    .nl-right { padding-top: 0; }
  }

  @media (max-width: 560px) {
    .posts-grid { grid-template-columns: 1fr; }
    .foot-in { flex-direction: column; align-items: flex-start; }
    .hl-line { font-size: clamp(2rem, 10vw, 2.8rem); }
    .hl-accent { font-size: clamp(2.4rem, 12vw, 3.4rem); }
    .hl-small { font-size: clamp(1.4rem, 7vw, 2rem); }
    .hero-stats { flex-wrap: wrap; width: 100%; }
    .hstat-sep { display: none; }
    .hero-actions { flex-direction: column; align-items: flex-start; }
    .hero-features { gap: .4rem; }
    .hf-sep { display: none; }
  }
</style>
